<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 
<mapper namespace="com.pet.p1.cart.CartDAO">


	<!-- cartList -->
	<select id="cartList">
	
		select id,cnum,cart.productnum,camount,productname,price,point
		from cart inner join product on(cart.productnum = product.productnum)
		where id=#{id}
	
	</select>



</mapper>